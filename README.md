## Add the following values into your localStorage

- `existingNames` - names which pics your have in `pages/custom-element/pics` folder. Format: `["john", "doe"]`
- `restrictedNames` - names which are restricted. Format: `["john", "doe"]`
